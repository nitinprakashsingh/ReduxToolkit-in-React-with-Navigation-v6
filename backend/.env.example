NODE_ENV=production
PORT=5000
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE?schema=public"
# Comma-separated list of deployed Hospital panel origins. Do not include a trailing slash.
CLIENT_URLS="https://admin.example.com"
# Generate with: openssl rand -base64 48
JWT_SECRET="replace-with-a-unique-48-byte-secret"
JWT_EXPIRES_IN="8h"

# Only needed when running: npm run admin:seed
ADMIN_EMAIL="nitinprakashsingh2023@gmail.com"
ADMIN_PASSWORD="replace-with-a-strong-password"
