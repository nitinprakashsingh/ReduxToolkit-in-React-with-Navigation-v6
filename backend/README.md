# Hospital Panel Backend

Express + TypeScript + Prisma backend for the hospital web panel.

## Setup

```bash
cd backend
npm install
copy .env.example .env
npm run dev
```

Health check:

```txt
http://localhost:5000/api/health
```
